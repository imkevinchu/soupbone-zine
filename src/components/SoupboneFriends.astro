---
import { getCollection } from "astro:content";

const soupbonefriends = await getCollection("soupbonefriends");
---

<style>
  ul {
    list-style-type: none;
    padding: unset;
  }
  .grid-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    column-gap: 1.6em;
    row-gap: 1em;
  }
  .soupbonefriends {
    display: grid;
    grid-template-columns: 1fr 1fr;
    column-gap: 0.25em;
  }
  @media (max-width: 768px) {
    .grid-wrapper {
      display: grid;
      grid-template-columns: 1fr;
      column-gap: 0em;
      row-gap: 1em;
    }
  }
  figcaption {
    text-align: left;
  }
</style>

<section>
  <ul class="grid-wrapper">
    {
      soupbonefriends.map((soupbonefriends) => (
        <li class="soupbonefriends">
          <div>
            <img src={soupbonefriends.data.image} alt="" />
          </div>
          <div>
            <h5>{soupbonefriends.data.name}</h5>
            <figcaption>{soupbonefriends.data.description}</figcaption>
          </div>
        </li>
      ))
    }
  </ul>
</section>
